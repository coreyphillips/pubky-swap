/target
**/*.rs.bk
.DS_Store
*.pkarr
.idea
.vscode

# Persisted in-flight swap state (provider --data-dir)
/pubky-swap-data/
/provider-data/

# Node credentials — never commit these (the self-hosting guide / regtest CI place them locally)
*.macaroon
*.cert
*.key
*.pem
